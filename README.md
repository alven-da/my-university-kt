## My University (Pet Project)

A monorepo of microservices with an API gateway that serves as a middleware.

My attempt to implement a back-end service using Kotlin / Spring Boot


### Environment variable

The `.env` should contain the ff:

```
# For DB
DB_USERNAME=
DB_PASSWORD=

# For Auth Seeding
CLIENT_ID=
CLIENT_SECRET=
CLIENT_REDIRECT=
```